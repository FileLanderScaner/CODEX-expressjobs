import { AppShell } from "@/components/app-shell";

export default function TermsPage() {
  return (
    <AppShell>
      <main className="mx-auto max-w-3xl px-4 py-10">
        <h1 className="text-3xl font-black">Terminos</h1>
        <p className="mt-4 leading-7 text-[var(--muted)]">ExpressJobs MVP conecta clientes y trabajadores para coordinar tareas locales. No promete empleo, ingresos garantizados ni pagos live en esta etapa.</p>
      </main>
    </AppShell>
  );
}
